@page "/"

<PageTitle>Esmeralda — Tarot of the Wandering Stars</PageTitle>
<div id="constellationContainer">
    <canvas id="constellationCanvas"></canvas>
</div>
<div class="index-header-row">
    <h1 class="esm-question-title">Hello Seeker</h1>
    <a href="/about" class="index-about-link">About</a>
</div>

<div class="row g-2 justify-content-center tarot-card-row">
    <div class="col-4 tarot-card-col">
        <figure class="tarot-card">
            <img src="@card1" class="my-image img-fluid" alt="@card1name">
            <figcaption class="text-center">@card1name</figcaption>
        </figure>
    </div>
    <div class="col-4 tarot-card-col">
        <figure class="tarot-card">
            <img src="@card2" class="my-image img-fluid" alt="@card2name">
            <figcaption class="text-center">@card2name</figcaption>
        </figure>
    </div>
    <div class="col-4 tarot-card-col">
        <figure class="tarot-card">
            <img src="@card3" class="my-image img-fluid" alt="@card3name">
            <p class="text-center">@card3name</p>
        </figure>
    </div>
</div>

    
<div class="m-3">
    <InputText @bind-Value="@enteredText"
               @onkeydown="HandleQuestionKeyDown"
               Disabled="@disabled"
               Placeholder="What do you seek?"
               class="w-100 question-input" />
</div>

<div class="m-3">
    <button class="btn btn-esmeralda" type="button" @onclick="LoadCards" disabled="@disabled">
        @if (isLoading)
        {
            <span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>
            <text>Reading your cards...</text>
        }
        else
        {
            <text>Ask Esmeralda</text>
        }
    </button>
</div>
<div class="m-3">
    <div id="responseArea" class="esm-response-area" style="display:@(esmeraldaResponse is not null ? "block" : "none")">
        <p class="esm-response-text">@esmeraldaResponse</p>
        @if (esmeraldaResponse is not null)
        {
            <div class="mt-3">
                <a class="btn btn-esmeralda"
                   href="https://www.buymeacoffee.com/paradigmbi"
                   target="_blank"
                   rel="noopener noreferrer">
                    Cross my hand with silver
                </a>
            </div>
        }
    </div>
</div>
<style>
    @@import url('https://fonts.googleapis.com/css2?family=Cookie&display=swap');
    @@import url('https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600&family=Cinzel+Decorative:wght@400&family=Lora:ital,wght@0,400;1,400&display=swap');
    @@import url('https://cdn.jsdelivr.net/npm/@@tabler/icons-webfont@@latest/tabler-icons.min.css');

 
    .btn-esmeralda {
        font-family: 'Cinzel Decorative', serif;;
        font-size: 1.7rem;
        background-color: #7b2cbf;
        color: #f4d35e;
        border-color: #7b2cbf;
    }

    .btn-esmeralda:hover,
    .btn-esmeralda:focus {
        background-color: #6a24a6;
        border-color: #6a24a6;
        color: #f7dc6f;
    }

    .btn-esmeralda:disabled {
        background-color: #7b2cbf;
        border-color: #7b2cbf;
        color: #e9c94f;
        opacity: 0.65;
    }
    /* ── Header ──────────────────────────────────── */
    .esm-header {
        text-align: center;
        margin-bottom: 2.5rem;
    }

    .esm-title {
        font-family: 'Cinzel Decorative', serif;
        font-size: clamp(1.6rem, 5vw, 2.4rem);
        color: #7b2cbf;
        letter-spacing: 0.05em;
        margin: 0 0 0.3rem;
        font-weight: 400;
    }

    /* ── Response area ───────────────────────────── */
    .esm-response-area {
        background: rgba(20, 10, 45, 0.55);
        border: 1px solid rgba(180, 150, 255, 0.2);
        border-radius: 10px;
        padding: 1.5rem;
        animation: fadeIn 0.6s ease;
    }

    @@keyframes fadeIn {
        from { opacity: 0; transform: translateY(6px); }
        to   { opacity: 1; transform: translateY(0); }
    }

    .esm-response-text {
        font-family: 'Lora', serif;
        font-style: italic;
        font-size: 1rem;
        color: #ddd0f5;
        line-height: 1.8;
        margin: 0;
    }

    .tarot-card-row {
        margin-inline: auto;
        max-width: 64rem;
    }

    .tarot-card-col {
        display: flex;
        justify-content: center;
    }

    .tarot-card {
        width: min(100%, 16rem);
        margin: 0;
    }

    .tarot-card .my-image {
        width: 100%;
        max-width: 100%;
        height: auto;
    }

    .index-header-row {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 0.5rem;
        gap: 1rem;
    }

    .index-about-link {
        color: #9fe8df;
        text-decoration: none;
        font-family: 'Cinzel', serif;
        font-size: 0.95rem;
        letter-spacing: 0.04em;
    }

    .index-about-link:hover {
        text-decoration: underline;
    }

    .esm-question-title {
        font-family: 'Lora', serif;
        font-size: 1.4rem;
        color: darkolivegreen;
        margin: 0;
    }

    .question-input {
        font-family: 'Lora', serif;
        font-size: 1.1rem;
        color: darkolivegreen;
    }

    .question-input::placeholder {
        font-family: 'Lora', serif;
        font-size: 1.1rem;
        color: darkolivegreen;
        opacity: 1;
    }
</style>
<script>
    window.zodiacConstellations = window.zodiacConstellations || [
        { name: 'Aries', stars: [{x:0.12,y:0.18},{x:0.18,y:0.22},{x:0.22,y:0.20}], lines: [[0,1],[1,2]] },
        { name: 'Taurus', stars: [{x:0.30,y:0.12},{x:0.34,y:0.18},{x:0.38,y:0.14},{x:0.42,y:0.22}], lines: [[0,1],[1,2],[2,3]] },
        { name: 'Gemini', stars: [{x:0.55,y:0.10},{x:0.58,y:0.16},{x:0.62,y:0.12},{x:0.64,y:0.20}], lines: [[0,1],[1,2],[2,3]] },
        { name: 'Cancer', stars: [{x:0.82,y:0.16},{x:0.85,y:0.22},{x:0.88,y:0.18}], lines: [[0,1],[1,2]] },
        { name: 'Leo', stars: [{x:0.18,y:0.42},{x:0.22,y:0.48},{x:0.24,y:0.42},{x:0.28,y:0.50}], lines: [[0,1],[1,2],[2,3]] },
        { name: 'Virgo', stars: [{x:0.41,y:0.36},{x:0.45,y:0.43},{x:0.49,y:0.39},{x:0.52,y:0.46}], lines: [[0,1],[1,2],[2,3]] },
        { name: 'Libra', stars: [{x:0.70,y:0.40},{x:0.74,y:0.46},{x:0.78,y:0.42}], lines: [[0,1],[1,2]] },
        { name: 'Scorpio', stars: [{x:0.84,y:0.38},{x:0.88,y:0.44},{x:0.92,y:0.39},{x:0.96,y:0.46}], lines: [[0,1],[1,2],[2,3]] },
        { name: 'Sagittarius', stars: [{x:0.15,y:0.75},{x:0.20,y:0.80},{x:0.25,y:0.74},{x:0.30,y:0.82}], lines: [[0,1],[1,2],[2,3]] },
        { name: 'Capricorn', stars: [{x:0.40,y:0.68},{x:0.44,y:0.74},{x:0.48,y:0.70}], lines: [[0,1],[1,2]] },
        { name: 'Aquarius', stars: [{x:0.60,y:0.72},{x:0.64,y:0.78},{x:0.68,y:0.74},{x:0.72,y:0.80}], lines: [[0,1],[1,2],[2,3]] },
        { name: 'Pisces', stars: [{x:0.80,y:0.68},{x:0.84,y:0.74},{x:0.88,y:0.70}], lines: [[0,1],[1,2]] }
    ];

    function setupConstellationCanvas() {
        const canvas = document.getElementById('constellationCanvas');
        const parent = document.getElementById('constellationContainer');
        if (!canvas || !parent) return;
        const ctx = canvas.getContext('2d');
        if (!ctx) return;

        function resize() {
            const dpr = window.devicePixelRatio || 1;
            const width = parent.clientWidth;
            const height = parent.clientHeight;
            canvas.style.width = width + 'px';
            canvas.style.height = height + 'px';
            canvas.width = Math.floor(width * dpr);
            canvas.height = Math.floor(height * dpr);
            ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
            draw();
        }

        function draw() {
            const width = parent.clientWidth;
            const height = parent.clientHeight;
            ctx.clearRect(0, 0, width, height);
            ctx.strokeStyle = 'rgba(255,255,255,0.22)';
            ctx.fillStyle = 'rgba(255,255,255,0.9)';
            ctx.lineWidth = 1;

            window.zodiacConstellations.forEach(constellation => {
                constellation.lines.forEach(([a, b]) => {
                    const from = constellation.stars[a];
                    const to = constellation.stars[b];
                    ctx.beginPath();
                    ctx.moveTo(from.x * width, from.y * height);
                    ctx.lineTo(to.x * width, to.y * height);
                    ctx.stroke();
                });

                constellation.stars.forEach(star => {
                    const x = star.x * width;
                    const y = star.y * height;
                    ctx.beginPath();
                    ctx.arc(x, y, 2.5, 0, Math.PI * 2);
                    ctx.fill();
                });
            });
        }

        window.addEventListener('resize', resize);
        resize();
    }

    if (document.readyState === 'complete' || document.readyState === 'interactive') {
        setupConstellationCanvas();
    } else {
        window.addEventListener('load', setupConstellationCanvas);
    }
</script>

